[package]
name = "cuda-causal-graph"
version = "0.1.0"
edition = "2021"
description = "Directed acyclic causal graph for tracking inference chains and counterfactual reasoning."
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
